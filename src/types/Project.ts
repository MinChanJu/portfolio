export type Project = {
  project: string;
  period: string;
  team: string;
  role: string;
  skills: string;
  source?: string;
  back?: string;
  front?: string;
  site?: string;
  description: string;
  name: string;
}